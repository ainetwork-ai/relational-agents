node_modules/
.next/
.next-e2e/
# NEXT_DIST_DIR variants — a second dev server needs its own build dir
.next-*/
.env*
!.env.example
*.tsbuildinfo
test-results/
playwright-report/
notion-fs/
app/e2e/.okf-work/
.gstack/

# runtime/session state — never commit
.omc/
app/.omc/
.playwright-mcp/
md-mirror/

# personal/runtime data — caches, raw originals, generated content.
# user uploads are runtime data, not source
app/public/uploads/
app/public/avatars/
.claude/scheduled_tasks.lock

# production runtime state — the live OKF content tree and DB dumps.
# Lives in the project so deploys are discoverable, but is data, not source.
/deploy/

# local backups (not source)
/backup/

# Demo videos live on YouTube, not in git
assets/*.mp4
*.mp4

# 노션 대조용 DOM 캡처 — 로컬 참고 자료이고 소스가 아니다.
# 파일 하나가 최대 1.4MB 라 리포를 무겁게 만든다. 무엇이 어떤 화면인지는
# docs/notion-captures.md 에 정리돼 있다.
docs/*.html
docs/plus_page_personalpage_html
